---
// レイアウト読み込み
import BaseLayout from "../../components/BaseLayout.astro"
// 記事データを取得
import { getCollection } from "astro:content";
// jaコレクションの全記事を取得
const entries = await getCollection("ja");
// カテゴリの重複を排除してリスト化
const categories = [...new Set(entries.map(e => e.data.category))];

// カテゴリ名と日本語表記の対応表
const catnames: Record<string, string> = {
	"peskin": "[Peskin] 場の量子論 ノート",
}

// props
const lang = "ja";
const type = "website";
const title = "Home";
const description = "理論物理学に関するノートをまとめています。";
---
<!-- レイアウト呼び出し -->
<BaseLayout lang={lang} type={type} title={title} description={description}>
	<!-- タイトル -->
	<h1>{title}</h1>
	<!-- 説明 -->
	<p>{description}</p>
	<!-- カテゴリー -->
	<h2>Notes</h2>
	<ul>
		{categories.map(cat => (
			<li>
				{/* リンクを日本語表記 */}
				<a href={`/${lang}/${cat}/`}>{catnames[cat]}</a>
			</li>
		))}
	</ul>
</BaseLayout>
